# /lint — Run linters

Run all linters for this project and report issues.

1. Run `npm run lint` from the `frontend/` directory to lint the React code
2. Report any ESLint errors or warnings
3. If `$ARGUMENTS` is `--fix`, run `npm run lint -- --fix` to auto-fix issues where possible
4. Summarize what was fixed and what needs manual attention
